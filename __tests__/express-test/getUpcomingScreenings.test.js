import {describe, it, jest} from '@jest/globas';

import { fetchScreeningsMovie, upcomingScreenings } from '../../src/backendJs/fetchScreening';

//TODO: test for fetchScreeningMovie(id)
// test fetchScreeningsMovie(id) returns empty array if no screening in api for movie.
// returns just the screenings for a specific movie if there a screenings for several movies.
//Gives empty list if @param {*} id does not exist.

//TODO: tests for upcomingScreenings

