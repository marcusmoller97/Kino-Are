
doctype html
html
  head
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    each item in menuItems
      if item.active
        title #{item.label}
        break
        if item.label == 'Movie'
          script(type="module", src="/js/fetchFromAPI.js", defer)
        else
          if item.label !== 'Movies'
            script(type="module", src="/js/main.js", defer)
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(rel="stylesheet", type="text/css", media="screen", href="/static/main.css")
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    script(src="https://kit.fontawesome.com/8357c99e29.js", crossorigin="anonymous")
    script(type="module", src="/js/password.js", defer)

    <!-- The script above needs to be added on every single HTML file that contains movie cards -->
  body
    header
      img.kinoLogo(src="/pictures/logoHead.png", alt="logotyp för Kino", usemap="#logoMap")
      map(name="logoMap")
        area(shape="rect", coords="225,10,573,143", href="/home", alt="Kino")
      a(class='button' class="loginBtn" href='/login') Login
      .navContainer
        nav.headerNav
          each ele in menuItems
            if ele.label !== 'Movie'
              a.headerNavItem(href=ele.link) #{ele.label}
        .searchBar
          input.searchInput(type="search", placeholder="Search...")
          button.searchBtn Search
      figure.cinemaSeats
        img.picUnderNav(src="/pictures/seats.png", alt="Image under the navbar not found!")



block content
   form.loginForm
      h1 Logga in!
      input.form-control#email(type="email" placeholder="Epostadress")
      input.form-control#password(type="password" placeholder="Lösenord")
      div#succesLogin.alert.alert-succes.bg-succes(style="background-color: #14C400; display: none")
        h4.alert-heading  Login succesfull
      div#unsuccesLogin.alert.alert-danger(style="background-color: #DE8585; display: none")
        h4.alert-heading(style="color: #701813")  Login unsuccesfull

      button.btn.btn-primary.loggaInBtn(type="submit") Logga in
      a(href="/signUp") Skapa konto

script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")


footer
    nav
        a.footerNav.footerAboutUsLink(href="#") Om oss
        a.footerNav.footerAllMoviesLink(href="#") Alla filmer
        a.footerNav.footerInstagramLink(href="#") Instagram
        a.footerNav.footerFacebookLink(href="#") Facebook
        a.footerNav.footerXLink(href="#") X
        a.footerNav.footerTosLink(href="#") ToS
        a.footerNav.footerPrivacyPolicyLink(href="#") Privacy policy
